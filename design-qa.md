# 主站强化设计检查

## 2026-09-20：减少模板感与真实多屏交互

本节记录本次续作的实际证据；下方保留早期迭代历史，不将历史验证当成本轮结果。范围为主站，未修改 `public/sites/`，未发布到公网。

### 本轮审查与实现

1. 首屏：保留用户已认可的蓝紫玻璃、真实设备截图与构图，增加小幅指针层次响应；无持续漂浮或新的暂停控件。当前截图：`qa/polish-home-1440.png`、`qa/polish-home-390.png`。
2. 案例区：移除包围整区的渐变圆角卡片，改为开放排版、横向标题与细线索引；保留三个标签和到位即停的滑块。图片解码成功后统一提交标签、图片及文案，重复当前标签不重播，快速选择防止旧结果回填。证据：`qa/02-audit-showcase-desktop.png`（改前）、`qa/polish-showcase-1440.png`、`qa/polish-showcase-390.png`（改后）。
3. 多屏体验：点击后创建真实客户站 iframe，可用预设、原生滑杆、右侧把手及键盘调宽；预设使用 440ms 单向缓出，实际 iframe 宽度变化触发真实断点。宽屏在可用空间内等比例显示，界面明确说明缩放。`qa/polish-live-preview-tablet-1440.png`、`qa/polish-live-preview-390.png`。
4. 内容区：去除双卡片外框，改用错落作品图、开放正文和职责分隔线；宋体只用于少数强调标题，正文保留现有中文无衬线。宣传式标题改为具体内容。`qa/polish-details-1440.png`、`qa/polish-details-390.png`。
5. 流程与收尾：保留已有可展开流程，增加图片解码及竞态保护；收尾改为开放式大字排版。`qa/polish-process-1440.png`、`qa/polish-closing-1440.png`。

### 实际验证

- 使用 Codex 内置浏览器，桌面请求 1440×960；手机请求 390×844，浏览器实际返回 391×845（设备像素取整），不声称精确 390 CSS px。
- 320、390、768、1024、1440 五档请求宽度，home/cases/plans/intake/preview/published 六路由，共 30 组合：无横向溢出，无已完成加载却解码失败的图片。原始结果见 `qa/polish-responsive-matrix.json`。这不是 30 张逐页像素验收。
- 首页初始 iframe 数为 0，开启为 1，关闭与离页为 0；同页跳到流程保留预览 ID、宽度及展开状态。
- 桌面→手机过渡采样：1440→1207→390 px；结束后 iframe 样式宽、滑杆与输出均为390。平板预设三者均为768，修正原先 10px 步长会把768取整为770的不一致。
- 把手拖动约100屏幕像素，居中视口宽度768→970、右边缘约移动101px；Home 到360，ArrowRight 到370。确认居中几何已计入拖动。
- 手机预览可用，开关后焦点回到入口；案例标签 End 到移动体验，选中态与内容一致。
- 流程切换仅一项展开，预览图片/标题与所选阶段同步；路由切换焦点落到新标题。程序聚焦标题不再产生整块装饰性描边。
- 手机菜单 Escape 关闭并回焦点；套餐确认弹窗、案例抽屉 Escape 关闭并回到触发按钮；案例筛选同步 aria-pressed 和可见结果。
- 浏览器采集的 error/warn 日志为空。真实多屏 iframe 的简历新窗口与下载权限已按客户页实际入口配置，未在本轮触发下载。
- `npm run check`、`npm run build` 通过；客户目录 Git diff/status 为空。新增动效、图片异步、iframe、observer、监听和 RAF 均有卸载路径，独立代码复核后修正拖动几何和预览出口权限。

### 验证边界

本轮已检查减少动态的 JS/CSS 实现和实时监听，但未通过内置浏览器切换系统减少动态偏好；未进行真实手机 GPU/耗电测试、Safari/Firefox、完整读屏或 Lighthouse 审计。手机采用内置浏览器尺寸覆盖，不等同真机。表单与发布仍是已有静态 MVP 演示，未增加后端或公网部署。

当前构建与上述主流程验收通过；主观视觉效果可在本地预览查看。

日期：2026-09-19。范围仅限主站；`public/sites/zhang-hong/` 的源码、内容和素材均未修改。

final result: passed

## 视觉依据与对照

- 用户最新要求：去掉过度简约感，加强蓝紫色视觉冲击力、材质层次、前端设计能力与真实交付的说服力。原先浅色方案已被取代。
- source visual truth：`qa/studio-design-reference.png`（1536 × 1024，内置 Image Gen 生成的强化方向稿）。
- implementation：`qa/final-home-1536-reference-comparison.png`（1536 × 1024，CSS viewport 同尺寸，deviceScaleFactor 1）。
- route：`http://127.0.0.1:4173/#/home`，深色品牌主题，顶部初始状态，无弹窗。
- 两张等尺寸图已在同一次工具结果中作为配对视觉输入检查；未宣称这是拼接的左右对照图。
- 局部检查：`qa/final-home-1366-first-screen.png`、`qa/final-showcase-mobile-tab.png`；390 × 844 手机首屏及各路由完整截图位于 `qa/final-*-390.png`。
- 浏览器：独立、临时上下文的本地 Edge / Playwright。不是已连接的 Codex 内置浏览器，也未使用用户登录资料。

## 五项视觉检查

| 检查面 | 结果 |
| --- | --- |
| 字体与层级 | 中文无衬线；标题加重、两行构图；正文和辅助文字按层级分开。1366 × 768 首屏主按钮可见，320 宽标题不溢出。 |
| 布局与间距 | 大作品与手机叠放建立视觉重点；案例切换、作品拼图、内容示例、可展开流程采用不同结构；表单保留清晰阅读节奏。 |
| 配色与材质 | 深靛蓝主底、蓝紫强调色；玻璃边缘、折射图像与实际浏览器截图组合，所有主站路由使用一致色系。 |
| 图像质量 | 首屏、作品和移动端截图来自实际客户页面，只读截取到主站 assets。背景为独立生成图，不以整张 UI 图片替代网站。影视/游戏图片明确为客户项目素材，不伪称服务方制作。 |
| 文案与真实性 | 未添加虚假客户数量、合作品牌、评价或效果数据；示意方向与真实交付案例分开。发布流程明确为本地预览，不再显示虚构线上地址或不可扫描的仿二维码。 |

## 对照历史与修复

1. 原浅色方案：用户指出过于简约、缺少视觉冲击与信服力。已改为新的空间玻璃视觉，增加可操作案例讲解和明确的交付内容。
2. [P2，已修复] 首轮实现中桌面作品容器宽度 112%，压住下方案例说明。缩至 100%，重拍 1536 × 1024 对照图；说明与入口不再被遮挡。
3. [P2，已修复] 切换主站为深色后，预览页旧颜色选项可能产生深色文字。已为四种选项设置深底可读的强调色，并修复跳转链接的前景色。
4. [P2，已修复] 弹窗/抽屉原本缺少焦点约束。现在打开后背景 inert，Tab 循环、Escape 关闭并恢复触发器焦点；实际浏览器验证通过。
5. 首次动效测试在过渡开始时检查 opacity 为 1 导致断言失败；改为等待可见过渡完成后复查。无需改变正常动效，最终滚动显现测试通过。

## 有意保留的差异

- 方向稿是构图参考，不是要求逐像素复刻的客户设计稿。网页保留原品牌名、真实客户截图与既有路由；没有采用生成图中的虚构客户域名、英文装饰标语、滚动提示或岩石背景。
- 实现中的设备比例略小，留出案例署名和真实入口；首屏下方增加服务范围，回答“到底提供什么”，随后进入详细案例。
- 案例截图的内容完全取自现有客户站，不为了贴合生成稿而修改客户页面。
- 主站使用明确的深色品牌视觉；未增加独立浅色模式。减少动态设置下内容直接显示，减少透明度时导航使用实色。

## 验证记录

- 六个路由：home、cases、plans、intake、preview、published。
- 五档宽度：320、390、768、1024、1440；30 个组合均无横向溢出、无坏图。
- 1366 × 768：主按钮首屏可见。
- 案例筛选与抽屉、套餐确认弹窗、手机菜单均经过实际操作。
- 五步表单必填提示、输入自动保存、刷新恢复、修改意见、本地确认与访客预览均经过实际操作。
- 清空草稿要求二次确认；独立测试上下文中验证，不影响用户浏览器草稿。
- 新增案例首屏 / 作品 / 手机切换、左右方向键导航、服务流程展开均通过。
- 减少动态模式、滚动入场、弹窗焦点循环与返回均通过。
- 记录到的主站 JavaScript pageerror：0。
- `npm run check`、`npm run build` 通过。客户目录 Git diff 与状态均为空。

## 边界与后续

这是已验证的主站前端迭代，不是后端订单、上传、发布或隐私认证完成报告。当前图片只记录文件名，文本保存在本机浏览器，正式交付需接入服务端。未执行真实设备 Safari/Firefox、完整屏幕阅读器或 Lighthouse 性能审计；不宣称已取得 Core Web Vitals 分数。静态素材体积与真机性能可在部署前单独优化。

## 生成素材记录

内置 Image Gen 使用记录（未使用 CLI/API 密钥）：

- 方向稿：`qa/studio-design-reference.png`。提示目标：在被否定的浅色首页与真实客户截图基础上，制作 1536 × 1024 的深靛蓝、蓝紫、折射玻璃、多端作品叠放首屏；保留码页集品牌、真实案例，不添加虚构指标与评价。
- 页面素材：`public/assets/studio-glass-background.png`。最终提示：以方向稿为参考，移除全部文字、导航、按钮、设备与网站截图，只保留深靛蓝空间及右侧、下方的蓝紫折射玻璃带；左侧留深色文本区，暗玻璃地面，无岩石、人物、文字、球体或假界面。
- 客户网站截图位于 `public/assets/zhang-hong-*-preview.png`，仅为主站展示使用，不修改客户站。

## 实施清单

### 追加：标签到位即停（2026-09-19）

用户明确取消到位后的回弹。仅修改`mountShowcaseIndicator`：由560ms阻尼弹簧改为380ms单向三次缓出，滑动期间轻微拉伸，抵达目标时同步恢复形状；再次点击已选中标签不重播动画。服务说明的悬停动效不受影响。

浏览器连续采样确认位置单调趋向目标、不越过、不反向、最后精确对齐；390与1440宽度左右切换、再次点击当前标签、减少动态模式立即定位通过。保留原标签布局、配色、文字、按压及键盘行为。静态检查与构建通过。

final result: passed

### 追加：案例标签Q弹与服务说明反馈（2026-09-19）

用户标注案例展示的三个切换标签，要求Q弹；追加标注首屏下方四项服务说明。仅修改主站相关组件，不修改客户站。

- 标签保持原尺寸、文字和键盘操作。独立玻璃选中块使用560ms阻尼弹簧关键帧，位移加轻微拉伸，文字和点击区域不随之扭曲；按压缩小至95%。
- 快速重定向读取当前动画矩阵再接续，ResizeObserver在断点变化时重新对齐；路由离开取消动画、断开观察器并删除媒体偏好监听。
- 测量第二项：目标x=98px，动画采样x依次4→104.866→99.2275→97.668→98，证明越过后回弹且最终精确对齐。连续五次快速切换后只保留一个选中块，最终内容与选中标签一致。
- 服务说明改为轻玻璃圆角分组，标题提升层级，桌面悬停抬升5px并以弹性曲线回落；保留为信息内容，不伪造可点击行为。手机两列排列。
- 首轮验证：方向键/Home切换、快速切换后最终对齐通过。后续五档宽度矩阵在对齐断言处失败，当时未完成三次路由往返检查；此前把计划检查记为已通过的记录已更正。
- [P2，已修复] 全局减少动态样式给所有元素添加`.01ms`过渡时间，滑块默认的`transition-property: all`因此产生一帧旧位置，与JS控制动画冲突。`studio.css`为滑块显式设置`transition-property: none`，让其位置只由WAAPI或静态赋值控制，不削弱普通模式回弹。
- 修复后重新跑完320/390/768/1024/1440五档宽度×三个标签，共15状态，点击后立即测量位置和宽度均对齐，无动画残留、无横向溢出。普通模式采样x=4→97.38→99.20→97.39→98（目标98），回弹后准确到位；快速重定向、方向键/End切换、动画中调整宽度、动画中打开减少动态设置均通过。三次home/plans路由往返确认滑块卸载、重新挂载后始终只有一个；pageerror=0。
- 证据：`qa/spring-tabs-desktop.png`和`qa/spring-tabs-mobile.png`是标签组件本身的元素截图，分别来自1440×900与390×844视口；`qa/service-proof-spring-desktop.png`为1440×1000视口、第二项悬停稳定态；`qa/service-proof-spring-mobile.png`为390×844视口。DPR均为1。标签源图是用户附件`C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-d160c444-8d4f-47fb-9aae-0f78b3ba11d3.png`，服务说明源图是同目录`codex-clipboard-30791881-0961-45dd-ac35-9473c3e82fae.png`。实际动效以中间帧测量验证，不以静态截图代替动画验收。文字与背景色保留既有风格，标签选中底板在静止时与原按钮对齐；服务说明由平铺文本改为可辨识分组，是用户要求的有意差异。
- 静态检查、构建通过；客户目录无diff。未做真机GPU帧率或Safari验证。

final result: passed

### 追加：流程胶囊与展开动效、入口重点、页脚（2026-09-19）

本轮沿用 Product Design 局部标注工作流和 design-taste-frontend 的保留式打磨：不改路由结构、首屏构图、客户站或已确认色系。用户提供的旧样式截图用于问题定位，不作为需要克隆的视觉稿。

来源：`C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-9528bf3e-1697-409b-a811-123d01135cbd.png`（1570×556，流程）、`codex-clipboard-0f9a3b04-6e24-48fe-989b-9ecbfb535a26.png`（1170×567，服务入口）、`codex-clipboard-8781013e-e537-4242-af18-0fd9936e00c2.png`（1206×587，约定及页脚，后两项同一Temp目录）。这些是裁切区域，不能与完整浏览器视口作像素相等判断。

实现证据：`qa/process-motion-desktop.png`、`qa/decision-focus-desktop.png`、`qa/footer-refined-desktop.png`，1440×1000 CSS px / DPR 1；`qa/process-motion-mobile.png`、`qa/decision-focus-mobile.png`、`qa/footer-refined-mobile.png`，390×844 CSS px / DPR 1。结合用户源图与上述实际输出逐区复查。区域本身已可清晰辨识标题、标签与按钮，无需另外裁切放大。

发现与处理：

- [P2，已修复] 展开的旋转加号看起来是关闭符号。改用真实“−”与“+”，不用旋转加号。语义按钮提供aria-expanded/aria-controls，收起区域aria-hidden并inert。
- [P2，已修复] 原生details瞬间开合，交互生硬。改为单展开的CSS grid行过渡（480ms），正文透明度/位移渐变，图像联动保持。连续点击能从当前过渡位置反向，不排队、不锁定操作。
- [P2，已修复] 阶段与结果缺少层级。阶段小胶囊、主标题、说明、交付物胶囊分层；选中项有更明确的边缘与底色，未选中项降低权重。
- [P2，已修复] 服务入口文字权重接近。改为“直接做网站”“先梳理内容”大标题，突出动作词和胶囊入口，辅助段落保留简短判断条件。
- [P2，已修复] 合作约定与页脚只是分散灰字。三个语义小标题组织约定内容；页脚增加明确品牌重心、制作入口和独立底部导航，保留所有原导航目的地。

字体/排版：中文系统字体与首屏保持一致，流程标题桌面20px/手机17px，说明14px，胶囊11–12px；入口标题桌面最高43px/手机32px。间距/形状：24px区块圆角，胶囊仅用于标签和动作，正文不套多重容器。颜色：保留蓝紫灰与通透导航，闭合项不与展开项争抢亮度。图像：沿用真实交付截图，无新增假资产。内容：保留交付边界与按需审核含义，未引入虚假价格或评价。

动效实际测量：第二项打开时高度0→96.25→167.48px，关闭时167.48→47.22→0px，说明展开及收起都有中间帧；测量间隔约120ms及450ms。连续切换六次后仅一项展开，预览与最后所选阶段一致；Home/方向键焦点移动、Enter/Space操作通过。减少动态模式下立即收起且内容inert，控制台pageerror为0。

响应式复查：320/390/768/1024/1440五档宽度，home/plans/cases/intake共20组合无横向溢出。服务入口可定位优化套餐；页脚“开始你的页面”进入资料收集。静态检查与构建通过，客户目录git diff/status为空。

旧图说明：本地静态预览没有热更新；同文档只更改hash不会重载脚本和样式。验收已使用新查询参数`?design=polish-2`完整加载，未将旧文档截图当作当前交付结果。

限制：未进行真机Safari、完整读屏或Lighthouse性能审计；视觉风格主观接受度待用户确认。其余路由只接受共享页脚变化，不宣称本轮重做了全部页面。

final result: passed

### 追加：配套区块与通透导航（2026-09-19）

本轮是用户截图标注的局部打磨，不是重新设计已经认可的首屏。保留首屏构图、蓝紫流动素材与 WebGL 文件；修改流程、服务入口、套餐，以及共享导航的玻璃材质和交互。张红客户目录无修改。

- 需求来源：用户附件 `codex-clipboard-e1972b5b-a865-4744-9060-bfb334467b48.png`（流程）、`codex-clipboard-247d9569-8519-4352-9553-b8f57b0ec351.png`（入口）、`codex-clipboard-53569a26-d672-4313-99b5-c43650bf1340.png`（套餐）、`codex-clipboard-2173b841-b6de-4ff1-bed1-4f8893deb980.png`（菜单）；均位于 `C:/Users/ADMINI~1/AppData/Local/Temp/`。这些是待改善证据，不是要求像素复刻的目标。
- 保留目标：用户认可的首屏截图 `C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-c7f12cb6-9022-43b7-94be-32036813a9d2.png`。同一浏览器、1440×1000 CSS px、DPR 1、减少动态模式下，`qa/refine-before-home.png` 与 `qa/refine-after-home.png` 在同一比较输入中查看：标题、按钮、设备、素材与下方服务栏的构图保持一致；导航透光是明确要求的变化，因此不声称整张图片像素相等。
- 区块证据：`qa/refine-process-1440.png`、`qa/refine-services-1440.png`、`qa/refine-plans-1440.png`；菜单实际悬停证据 `qa/refine-menu-768.png`，768×900 CSS px、DPR 1。服务区截图为1200px宽元素截图，套餐为1440px宽全页截图，不与原图错误地进行等尺寸像素比较。
- 手机采用390×844真实视口：`qa/refine-process-mobile-viewport.png`、`qa/refine-services-mobile-viewport.png`。早期元素长截图包含固定导航和截图扩展造成的遮挡，不作为真实视口缺陷证据；已重新在实际视口检查。套餐手机聚焦证据 `qa/refine-plans-390.png`。

发现与修正：

1. [P2，已修正] 流程只有空白与文字：增加真实交付案例视图、分阶段展开面板与交付结果层级。展开阶段可切换项目、桌面、手机预览；原客户文件只读引用。
2. [P2，已修正] 服务入口视觉相同：网页制作入口展示真实案例，内容梳理入口以信息主题区分；整块可点击并定位相应套餐，增加抬升、边缘亮度与箭头反馈。
3. [P2，已修正] 套餐长卡片阅读顺序弱：桌面改为横向的方案、内容、交付与行动三栏；窄屏按相同信息顺序堆叠。复查后将主要说明与服务清单提升至14px，交付信息提升至12px。报价保持按需求确认，不编造价格或销量。
4. [P2，已修正] 导航背景不通透，菜单没有反馈：导航背景移至独立毛玻璃层，12px blur、15%–35%透明底色，菜单独立18px blur。悬停/键盘聚焦提供高亮、阴影、箭头；无透明支持或减少透明偏好时采用实色降级。

五项视觉复查：字体保留系统中文字体，标题与正文层级明确；版式消除流程空洞与套餐顶部空白；颜色延续蓝紫、不新增暖白绿；图片沿用真实交付素材、不新增虚构案例；文案保留服务边界，新增内容均为流程说明而非业绩承诺。以上变更为截图指向的有意差异。

验证：

- 320、390、768、1024、1440五档宽度，home/plans/cases/intake共20组合，无横向溢出。
- 流程鼠标与键盘Enter展开，只有一个阶段展开，标题及预览同步切换。
- 服务入口定位优化套餐，三种套餐均能打开确认弹窗，Escape关闭后焦点返回对应按钮。
- 菜单悬停截图、Escape关闭、真实背景透明与模糊计算样式检查通过。
- 动态模式下WebGL就绪；切换减少动态后画布不显示。没有增加暂停按钮。
- 浏览器pageerror记录为0，静态检查和构建通过；客户目录git diff/status为空。

限制：浏览器为已获准使用的独立本地Edge，不宣称内置浏览器已连接；没有真机Safari或完整屏幕阅读器验收。本轮未重做案例页、资料表单或预览页的内容布局，只对共享导航做一致性更新。新设计的主观满意度仍需用户确认。

final result: passed

### 追加：玻璃流动（2026-09-19）

- 用户要求保留现有构图、增加流动感，随后明确不要暂停控件。本次仅增加主站首屏背景的 WebGL 纹理折射，不修改标题、设备截图、客户网站或图像源文件。
- `public/glass-flow.js` 使用原图的连续流场变形、亮边高光移动和轻微色散；没有整图缩放或重新生成案例图。`public/studio.css` 保留静态图在画布下面。
- 无暂停按钮。离屏/后台停止计算，返回继续；系统减少动态设置使用静态原图。图形初始化失败或上下文丢失时立即显示原图。
- 1440 × 1000 连续截图：`qa/flow-frame-a.png`、`qa/flow-frame-b.png`（相隔 3 秒），确认画面不同而主标题坐标一致。手机证据：`qa/flow-mobile.png`（390 × 844，等待首屏入场完成后拍摄）。
- 验证：动画正常运行时控制台无错误/警告，四次离开再回到首页均正常；离屏返回、减少动态切换、手机无横向溢出通过；模拟不支持 WebGL 与主动丢失图形上下文均退回原图。这里的预期降级会记录明确警告。
- 限制：目标绘制上限为 30 fps，像素倍率上限为桌面 1.25、手机 1；这不是全机型帧率保证，未进行真实手机耗电或 GPU 性能基准测试。
- 静态检查与构建通过，客户目录无改动。final result: passed。

- [x] 强化首屏构图与材质
- [x] 实际案例的可交互展示
- [x] 主站六页视觉一致性
- [x] 移动布局和键盘可访问性复查
- [x] 构建检查和客户目录零改动核对
